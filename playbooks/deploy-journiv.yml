---
- name: Deploy journiv stack to homelab-core
  hosts: homelab_core
  gather_facts: false
  become: true
  vars_files:
    - ../group_vars/all.yml
    - ../group_vars/homelab_core.yml

  tasks:
    - name: Ensure journiv application directory exists
      ansible.builtin.file:
        path: "{{ journiv_app_dir }}"
        state: directory
        mode: '0755'

    - name: Ensure journiv data directories exist
      ansible.builtin.file:
        path: "{{ item }}"
        state: directory
        mode: '0755'
      loop:
        - "{{ journiv_data_dir }}"
        - "{{ journiv_data_dir }}/app"
        - "{{ journiv_data_dir }}/postgres"
        - "{{ journiv_data_dir }}/valkey"

    - name: Detect Journiv appuser uid and gid from image
      ansible.builtin.command: >-
        docker run --rm --entrypoint id {{ journiv_image }} appuser
      register: journiv_appuser_identity
      changed_when: false
      failed_when: false

    - name: Set effective Journiv app uid and gid
      ansible.builtin.set_fact:
        journiv_app_effective_uid: >-
          {{
            (journiv_appuser_identity.stdout | regex_replace('^.*uid=([0-9]+).*$','\\1'))
            if journiv_appuser_identity.rc == 0
            else (journiv_app_user_fallback_uid | string)
          }}
        journiv_app_effective_gid: >-
          {{
            (journiv_appuser_identity.stdout | regex_replace('^.*gid=([0-9]+).*$','\\1'))
            if journiv_appuser_identity.rc == 0
            else (journiv_app_user_fallback_gid | string)
          }}

    - name: Show Journiv app uid/gid used for host directory permissions
      ansible.builtin.debug:
        msg: >-
          Using uid={{ journiv_app_effective_uid }} gid={{ journiv_app_effective_gid }}
          for {{ journiv_data_dir }}/app ownership.

    - name: Ensure journiv app writable subdirectories exist
      ansible.builtin.file:
        path: "{{ item }}"
        state: directory
        owner: "{{ journiv_app_effective_uid }}"
        group: "{{ journiv_app_effective_gid }}"
        mode: '0775'
      loop:
        - "{{ journiv_data_dir }}/app"
        - "{{ journiv_data_dir }}/app/media"
        - "{{ journiv_data_dir }}/app/logs"

    - name: Ensure journiv app data tree ownership and permissions are correct
      ansible.builtin.file:
        path: "{{ journiv_data_dir }}/app"
        state: directory
        owner: "{{ journiv_app_effective_uid }}"
        group: "{{ journiv_app_effective_gid }}"
        mode: '0775'
        recurse: true

    - name: Copy journiv docker-compose definition
      ansible.builtin.copy:
        src: ../files/homelab-core/journiv/docker-compose.yml
        dest: "{{ journiv_app_dir }}/docker-compose.yml"
        mode: '0644'

    - name: Copy journiv environment file
      ansible.builtin.copy:
        src: ../files/homelab-core/journiv/.env.example
        dest: "{{ journiv_app_dir }}/.env"
        mode: '0644'

    - name: Start journiv service with docker compose
      ansible.builtin.command: docker compose up -d
      args:
        chdir: "{{ journiv_app_dir }}"

    - name: Verify journiv endpoint returns 200
      ansible.builtin.uri:
        url: "{{ journiv_healthcheck_url }}"
        status_code: "{{ journiv_healthcheck_status }}"
        return_content: false
      register: journiv_health
      until: journiv_health.status == journiv_healthcheck_status
      retries: 20
      delay: 5
