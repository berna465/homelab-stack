# Deprecated Playbook Wrappers

This directory contains root-level playbook aliases that are no longer part of the
minimal supported compatibility surface.

Canonical entrypoints now live under:

- `infra/.../ansible/playbooks/`
- `platform/.../ansible/playbooks/`
- `apps/deploy/ansible/playbooks/`

These wrappers are preserved only as historical compatibility artifacts.
