# Application Deploy

This directory will contain application deployment logic.

Deployment logic should consume platform foundations through stable contracts and avoid environment-specific assumptions where possible.

Canonical role implementations now live under `apps/deploy/ansible/roles/`.

Canonical application playbook entrypoints now live under `apps/deploy/ansible/playbooks/`.
