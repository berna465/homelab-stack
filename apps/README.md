# Applications

This layer contains self-hosted application stacks and their deployment logic.

Applications must consume platform foundations cleanly and must not recreate host bootstrap or infrastructure provisioning behavior.
