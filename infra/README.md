# Infrastructure

Environment-specific substrate preparation lives here.

This layer is responsible for producing a reachable Linux host with the required storage and network prerequisites.

It must not define application deployment behavior.
