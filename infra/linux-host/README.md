# Linux Host Infrastructure

This directory will contain generic Linux host bootstrap assets.

The Linux host is the primary supported runtime target for the platform.

This layer should remain portable across bare metal, VMs, and future substrates.
