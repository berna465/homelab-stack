# role: reverse_proxy

Placeholder role directory for target repository layout alignment.
Canonical reverse proxy automation now lives under:

- `platform/services/ansible/playbooks/deploy-reverse-proxy.yml`
- `platform/services/stacks/infra-edge/`

This placeholder should not receive new primary logic.
