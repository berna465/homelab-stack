# role: nas_mounts

Placeholder role directory for target repository layout alignment.
NAS integration now belongs in:

- `infra/nas/ansible/playbooks/` for mount/integration
- `platform/foundations/ansible/tasks/` for runtime writable-path checks

This placeholder should not receive new primary logic.
