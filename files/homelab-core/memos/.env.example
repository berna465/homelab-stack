# Fixed host port for v1 direct access.
MEMOS_PORT=5230

# Keep a stable image tag to avoid surprise upgrades.
MEMOS_IMAGE=ghcr.io/usememos/memos:stable

# Persistent data location on the VM host.
MEMOS_DATA_PATH=/opt/memos/data
