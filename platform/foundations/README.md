# Platform Foundations

This directory will contain reusable Linux-native runtime foundations.

Examples:

- Docker and Compose prerequisites
- runtime directory conventions
- identity and permission model
- shared Docker networks
- host-level preflight checks

Foundations are shared runtime concerns, not infrastructure provisioning and not application deployment.
